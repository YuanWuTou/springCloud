server:
  port: 8905

spring:
  application:
     name: yuanwutou
  cloud:
    config:
      profile: dev
      label: config-label-test
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8881/eureka/

